//#show
//#disasm

void main()
	string a = "hallo"
	a += " Welt - "
	a += a
	a += a
	a += a
	a += a
	a += a
	a += a
	a += a
	a += a
	a += a
	a += a
	a += a
	a += a
	a.add('x')
	print a
	
	string s[]
	s = a.explode(" - ")
	
	for ss in s
		print ss
		ss.clear()
