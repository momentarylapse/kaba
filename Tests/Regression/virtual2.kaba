##show
##disasm

class Base
	virtual const void d()
		print("Base.d")

class Derived extends Base
	override void d()
		print("Derived.d")

void f(Base b)
	b.d()

void main()
	Base b
	Derived d
	f(b)
	f(d)
