
int[] a

void fill(out int[] x)
	for y in x
		if y == 5
			continue
		y += 100

void main()
	# a = 2
	#for int i, 0, 10
	#	a.add(i*2)
	#print a
	a = range(0,13)
	print(a)
	fill(a)
	print(a)
	
	for j in a
		j += 100
	
	for j in a
		print(j)
