#use x

class TraceData2
	var pos: vector
	var o: Model2*
	var t: Model2*

class Model2
	var pos: vector
	var object_id: int

void Trace2(vector a, vector b, TraceData2 td, bool c, int ignore)
	print(ignore)

void main()
	TraceData2 td
	vector p1, p2
	Model2 m
	m.object_id = 13
	Model2 *p = &m
	Trace2(p1, p2, td, false, p.object_id)
	
