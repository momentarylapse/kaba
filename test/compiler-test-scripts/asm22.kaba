#show
#disasm

func main()
	var f = 1.0
	#f = sin(0.1)
	#print f
	#float f = pi
	var j: int = f
	#print f2i(pi)
	vec3 v = vec3.0
	for i in 0:10000000
		j = f
		#f + f * f * f + f * (f - f * f)
		#v += v
