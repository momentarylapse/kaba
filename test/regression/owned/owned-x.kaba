class X
	func __init__()
		print("X.init")
	func __delete__()
		print("X.del")
	s: string


func main()
	var p: owned[X] = new X()
	p.s = "hallo"
	
#	print(p2s(p))
	
	if weak(p)
		print(p.s)
	print(repr(p))
	
	p = nil
	#del weak(give(p))
	
	if weak(p)
		print(p.s)
	else
		print("no object")
	print(repr(p))