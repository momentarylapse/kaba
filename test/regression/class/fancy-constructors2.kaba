class Node
	var a = "z"

struct Port
	func __init__(n: Node, name: string)
		node := &n
		print(name)
		print(node.a)

	var node: Node&


class X extends Node
	var name = "abc"
	var p = Port(self, a + "." + name)

func main()
	var x: X


