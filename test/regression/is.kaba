class Base
	func virtual x()

class Derived extends Base
	func override x()

func main()
	Base *a = new Base()
	Base *b = new Derived()
	if a is Derived
		print("a is Derived")
	if b is Derived
		print("b is Derived")
	print("done")
